import createTheme from "@mui/material/styles/createTheme";

const overrides = {
    palette: {
    },
    typography: {
    },
    components: {
    }
};

const theme = createTheme();

export default theme;